name: server
description: Recycle Vision backend — auth, AI prediction, deposit validation and points (JSON file store).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  crypto: ^3.0.3
  http: ^1.2.0
  path: ^1.9.0
  shelf: ^1.4.1
  shelf_multipart: ^2.0.0
  shelf_router: ^1.1.4
  shared:
    path: ../shared

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0