# Where a trained model artifact is expected when AI_SERVICE_CLASSIFIER=real.
# Drop model.onnx / model.tflite / model.pt here and set AI_MODEL_PATH=/app/models/model.onnx
# .gitignore keeps the directory clean; this file documents the contract.