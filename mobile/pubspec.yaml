name: recycle_vision
description: "Recycle Vision — AI-powered waste scanning connected to the EcoLoop Station."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.6.1

  # Networking (multipart AI upload, interceptor auth)
  dio: ^5.7.0

  # Realtime deposit status (falls back to HTTP polling when not available)
  web_socket_channel: ^3.0.1

  # Camera capture + gallery upload fallback
  camera: ^0.11.0
  image_picker: ^1.1.2

  # On-device SHA-256 for the Android camera E2E byte-identity proof
  crypto: ^3.0.6

  # Real-session token storage on-device
  flutter_secure_storage: ^9.2.4

  # Date/time formatting
  intl: ^0.19.0

  # Shared domain (models, routing + confidence policies)
  shared:
    path: ../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  async: ^2.13.0
  stream_channel: ^2.1.4
  integration_test:
    sdk: flutter
  path_provider: ^2.1.4

flutter:
  uses-material-design: true
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter.ttf
    - family: Tajawal
      fonts:
        - asset: assets/fonts/Tajawal-Regular.ttf
        - asset: assets/fonts/Tajawal-Medium.ttf
          weight: 500
        - asset: assets/fonts/Tajawal-Bold.ttf
          weight: 700