# EcoLoop local development broker.
# Anonymous/local only — this is a dev fixture, not a production broker.
listener 1883
allow_anonymous true

max_queued_messages 1000
message_size_limit 0

# Retain station state so the backend learns machine state even if it
# subscribes after the simulator has started.
persistence false